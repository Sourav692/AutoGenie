import { Token } from "@databricks/sdk-experimental/src/config/Token";
import { OidcEndpoints } from "@databricks/sdk-experimental/src/config/oauth/OidcEndpoints";
import { RequestInit, HeadersInit, fetch } from "@databricks/sdk-experimental/src/fetch";
export interface ClientOptions {
    clientId: string;
    clientSecret: string;
    useParams?: boolean;
    useHeader?: boolean;
    headers?: HeadersInit;
}
export declare class Client {
    private issuer;
    private options;
    constructor(issuer: OidcEndpoints, options: ClientOptions);
    requestResource(url: URL, token: Token, requestOptions: RequestInit): ReturnType<typeof fetch>;
    exchangeToken(params: Record<string, string | string[]>): Promise<Token>;
    private fetch;
}
//# sourceMappingURL=Client.d.ts.map